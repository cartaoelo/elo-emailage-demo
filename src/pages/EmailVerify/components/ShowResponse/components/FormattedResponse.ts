import styled from 'styled-components'

const FormattedResponse = styled.button`
	background: none;
	font-family: 'Poppins', sans-serif;
	font-size: 18px;
	margin: 30px 0 50px 0;
`

export default FormattedResponse
